# To run app

- `npm i` in root folder
- `npm run dev` in root folder

# To run test

- `npm run test` in root folder(**important**)

**You can customize tests, changing db schema and json schema**

## Make sure you added serialized json to db properly!
